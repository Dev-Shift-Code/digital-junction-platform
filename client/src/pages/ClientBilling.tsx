import { useMemo, useState } from "react";
import { Link } from "wouter";
import ClientAreaLayout from "@/components/ClientAreaLayout";
import { sampleCustomerInvoices } from "@/data/sampleClientJourney";
import { ChevronDown, Download, Eye, FileText, ReceiptText, Search, SlidersHorizontal } from "lucide-react";

type SortOrder = "recent" | "reference" | "title";

export default function ClientBilling() {
  const [search, setSearch] = useState("");
  const [sort, setSort] = useState<SortOrder>("recent");
  const [recordType, setRecordType] = useState("All");
  const records = useMemo(() => sampleCustomerInvoices.filter(invoice => `${invoice.title} ${invoice.reference} ${invoice.orderReference}`.toLowerCase().includes(search.trim().toLowerCase()) && (recordType === "All" || (recordType === "Service layout" ? invoice.title.toLowerCase().includes("service") : !invoice.title.toLowerCase().includes("service")))).sort((a, b) => sort === "reference" ? a.reference.localeCompare(b.reference) : sort === "title" ? a.title.localeCompare(b.title) : a.id.localeCompare(b.id)), [recordType, search, sort]);
  return <ClientAreaLayout><div className="space-y-6"><header><p className="eyebrow">Billing & invoices</p><h2 className="display mt-2 text-4xl text-[#1A312C]">Billing & Invoices</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-[#1A312C]/64">Review billing layouts connected to this customer account. Select the eye action on any row to open its detail preview.</p></header><div className="rounded-xl border border-[#428475]/20 bg-[#89D7B7]/18 px-4 py-3 text-xs leading-5 text-[#1A312C]/72"><strong className="text-[#1A312C]">Sample billing records:</strong> this page demonstrates the billing interface only. The entries are not issued invoices, paid transactions, tax documents, payment methods, or proof of any purchase.</div><section className="grid gap-4 sm:grid-cols-3"><article className="rounded-xl border border-[#1A312C]/10 bg-white/70 p-5"><ReceiptText className="size-5 text-[#428475]" /><p className="mt-5 font-mono text-[0.6rem] uppercase tracking-[0.1em] text-[#1A312C]/48">Sample billing records</p><p className="mt-1 text-2xl font-bold text-[#1A312C]">{sampleCustomerInvoices.length}</p></article><article className="rounded-xl border border-[#1A312C]/10 bg-white/70 p-5"><FileText className="size-5 text-[#428475]" /><p className="mt-5 font-mono text-[0.6rem] uppercase tracking-[0.1em] text-[#1A312C]/48">Sample amount display</p><p className="mt-1 text-sm font-bold text-[#1A312C]">Preview only</p></article><article className="rounded-xl border border-[#1A312C]/10 bg-white/70 p-5"><ReceiptText className="size-5 text-[#428475]" /><p className="mt-5 font-mono text-[0.6rem] uppercase tracking-[0.1em] text-[#1A312C]/48">Billing setup</p><p className="mt-1 text-sm font-bold text-[#1A312C]">Not connected</p></article></section><section className="grid gap-5 xl:grid-cols-[1fr_.38fr]"><div className="overflow-hidden rounded-[1.35rem] border border-[#1A312C]/10 bg-white/70"><div className="border-b border-[#1A312C]/10 p-5"><div className="flex flex-col gap-3 lg:flex-row"><label className="relative min-w-0 flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-[#428475]" /><input value={search} onChange={event => setSearch(event.target.value)} className="form-field !py-2.5 !pl-9 text-sm" placeholder="Search sample billing records" /></label><label className="flex items-center gap-2 rounded-xl border border-[#1A312C]/12 bg-white px-3 text-sm font-semibold text-[#1A312C]/65"><SlidersHorizontal className="size-4 text-[#428475]" /><span className="sr-only">Filter sample records</span><select value={recordType} onChange={event => setRecordType(event.target.value)} className="min-h-10 bg-transparent outline-none"><option>All</option><option>Service layout</option><option>Product layout</option></select></label><label className="flex items-center gap-2 rounded-xl border border-[#1A312C]/12 bg-white px-3 text-sm text-[#1A312C]/65">Sort <select value={sort} onChange={event => setSort(event.target.value as SortOrder)} className="min-h-10 bg-transparent font-semibold outline-none"><option value="recent">Latest preview</option><option value="reference">Reference</option><option value="title">Product title</option></select></label></div></div><div className="overflow-x-auto"><table className="w-full min-w-[46rem] text-left"><thead className="bg-[#FFF4E1]/70 font-mono text-[0.56rem] uppercase tracking-[0.09em] text-[#1A312C]/48"><tr><th className="px-5 py-4 font-medium">Sample record</th><th className="px-5 py-4 font-medium">Product</th><th className="px-5 py-4 font-medium">Display date</th><th className="px-5 py-4 font-medium">Sample amount</th><th className="px-5 py-4 font-medium">Status</th><th className="px-5 py-4 font-medium">Actions</th></tr></thead><tbody>{records.map((invoice, index) => <tr key={invoice.id} className="border-t border-[#1A312C]/10 bg-white/60"><td className="px-5 py-4"><p className="font-mono text-xs font-bold text-[#1A312C]">{invoice.reference}</p><p className="mt-1 text-[0.68rem] text-[#1A312C]/52">Sample record</p></td><td className="px-5 py-4"><div className="flex items-center gap-3"><span className={`size-10 shrink-0 rounded-lg bg-[linear-gradient(135deg,#1A312C,#428475)] ${index === 1 ? "!bg-[linear-gradient(135deg,#428475,#89D7B7)]" : ""}`} /><div><p className="text-sm font-bold text-[#1A312C]">{invoice.title}</p><p className="mt-1 text-xs text-[#1A312C]/55">Digital product · sample</p></div></div></td><td className="px-5 py-4 text-sm text-[#1A312C]/62">{invoice.date}</td><td className="px-5 py-4 text-sm font-semibold text-[#1A312C]">{invoice.amount}</td><td className="px-5 py-4"><span className="rounded-full bg-[#89D7B7]/35 px-2.5 py-1 font-mono text-[0.54rem] uppercase text-[#1A312C]/70">Preview only</span></td><td className="px-5 py-4"><div className="flex items-center gap-2"><button type="button" disabled title="Sample invoice document unavailable" className="grid size-9 place-items-center rounded-lg border border-[#1A312C]/10 text-[#1A312C]/35 opacity-60" aria-label={`Sample invoice document unavailable for ${invoice.title}`}><Download className="size-4" /></button><Link href={`/client/billing/${invoice.id}`} className="grid size-9 place-items-center rounded-lg border border-[#1A312C]/10 text-[#428475] transition hover:border-[#428475]/45 hover:bg-[#89D7B7]/18" aria-label={`View sample invoice detail for ${invoice.title}`}><Eye className="size-4" /></Link></div></td></tr>)}{!records.length && <tr><td colSpan={6} className="px-5 py-10 text-center text-sm text-[#1A312C]/60">No sample billing records match that search.</td></tr>}</tbody></table></div></div><aside className="h-fit rounded-[1.35rem] border border-[#1A312C]/10 bg-white/70 p-5 xl:sticky xl:top-6"><div className="flex items-center justify-between"><h3 className="display text-2xl text-[#1A312C]">Billing notes</h3><ChevronDown className="size-4 text-[#428475]" /></div><div className="mt-5 border-y border-[#1A312C]/10 py-5"><p className="font-mono text-[0.56rem] uppercase tracking-[0.09em] text-[#428475]">Customer billing setup</p><p className="mt-3 text-sm leading-6 text-[#1A312C]/64">No payment method, tax details, or billing address is connected to this sample customer view.</p></div><div className="mt-5 rounded-xl bg-[#89D7B7]/22 p-4"><p className="text-sm font-bold text-[#1A312C]">Need billing support?</p><p className="mt-2 text-xs leading-5 text-[#1A312C]/62">Contact Digital Junction when real billing records and documents are ready to be shared.</p></div></aside></section></div></ClientAreaLayout>;
}
