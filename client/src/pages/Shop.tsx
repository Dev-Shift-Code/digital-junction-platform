import { ArrowRight, Check, ChevronRight, Loader2, Search, Sparkles } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";
import PublicLayout from "@/components/PublicLayout";
import { trpc } from "@/lib/trpc";

function money(value: string | number) { return new Intl.NumberFormat("en-PH", { style: "currency", currency: "PHP", maximumFractionDigits: 2 }).format(Number(value)); }

export default function Shop() {
  const { data: products = [], isLoading, error } = trpc.portal.products.listPublished.useQuery();
  const [search, setSearch] = useState("");
  const [category, setCategory] = useState("All products");
  const categories = useMemo(() => ["All products", ...Array.from(new Set(products.map(product => product.category)))], [products]);
  const featured = products.filter(product => product.isFeatured);
  const filtered = products.filter(product => (category === "All products" || product.category === category) && `${product.title} ${product.summary} ${product.description ?? ""}`.toLowerCase().includes(search.toLowerCase()));
  return <PublicLayout><main><section className="overflow-hidden bg-[#1A312C] text-[#FFF4E1]"><div className="site-container grid gap-10 py-16 sm:py-20 lg:grid-cols-[1.05fr_.95fr] lg:items-center lg:py-24"><div><p className="font-mono text-[0.68rem] uppercase tracking-[0.15em] text-[#89D7B7]">Digital Junction products</p><h1 className="display mt-5 max-w-3xl text-5xl leading-[0.94] sm:text-6xl">Built by one studio, for people ready to <em className="font-normal text-[#89D7B7]">move.</em></h1><p className="mt-6 max-w-xl text-base leading-7 text-[#FFF4E1]/72">Curated templates, interface assets, and practical digital tools from Digital Junction Development Co. Only our studio publishes products here—there are no third-party sellers.</p><div className="mt-8 flex flex-wrap gap-3"><a href="#catalogue" className="button-primary buttonlike !bg-[#89D7B7] !text-[#1A312C] hover:!bg-[#FFF4E1]">Explore products <ArrowRight className="size-4" /></a><Link href="/contact" className="button-secondary buttonlike">Need something custom? <ChevronRight className="size-4" /></Link></div></div><div className="rounded-[1.65rem] border border-[#FFF4E1]/15 bg-[#FFF4E1]/8 p-5 sm:p-7"><div className="flex items-center justify-between border-b border-[#FFF4E1]/15 pb-5"><div><p className="font-mono text-[0.61rem] uppercase tracking-[0.13em] text-[#89D7B7]">The catalogue</p><p className="mt-2 text-sm text-[#FFF4E1]/68">Purposeful products, directly managed.</p></div><span className="grid size-11 place-items-center rounded-full bg-[#89D7B7] text-[#1A312C]"><Sparkles className="size-5" /></span></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl bg-[#FFF4E1] p-5 text-[#1A312C]"><p className="font-mono text-[0.6rem] uppercase tracking-[0.1em] text-[#428475]">Owner-operated</p><p className="display mt-3 text-3xl">One clear source.</p></div><div className="rounded-2xl border border-[#FFF4E1]/16 p-5"><p className="font-mono text-[0.6rem] uppercase tracking-[0.1em] text-[#89D7B7]">No marketplace</p><p className="mt-3 text-sm leading-6 text-[#FFF4E1]/72">Only Digital Junction listings appear in this space.</p></div></div><div className="mt-4 rounded-2xl bg-[#89D7B7]/16 p-5"><div className="flex items-start gap-3"><span className="grid size-7 shrink-0 place-items-center rounded-full bg-[#89D7B7] text-[#1A312C]"><Check className="size-4" /></span><p className="text-sm leading-6 text-[#FFF4E1]/80">Select a product, send your purchase request, and get delivery details directly from our team.</p></div></div></div></div></section><section id="catalogue" className="section-grid py-12 sm:py-16"><div className="site-container">{isLoading ? <div className="grid min-h-80 place-items-center"><Loader2 className="size-7 animate-spin text-[#428475]" /></div> : error ? <div className="py-20 text-center"><h2 className="display text-3xl">The catalogue is taking a moment.</h2><p className="mt-3 text-sm text-[#1A312C]/65">Please refresh and try again.</p></div> : products.length ? <><section className="mb-14"><div className="flex items-end justify-between gap-4"><div><p className="eyebrow">Featured from the studio</p><h2 className="display mt-3 text-4xl text-[#1A312C]">Start with the useful things.</h2></div><p className="hidden text-sm text-[#1A312C]/55 sm:block">{products.length} available product{products.length === 1 ? "" : "s"}</p></div>{featured.length ? <div className="mt-7 grid gap-5 lg:grid-cols-2">{featured.slice(0, 2).map(product => <article key={product.id} className="group overflow-hidden rounded-[1.45rem] bg-[#1A312C] text-[#FFF4E1]">{product.coverImageUrl ? <img src={product.coverImageUrl} alt={product.title} className="aspect-[1.65] w-full object-cover" /> : <div className="aspect-[1.65] bg-[linear-gradient(135deg,#428475,#1A312C)]" />}<div className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-[0.6rem] uppercase tracking-[0.1em] text-[#89D7B7]">{product.category}</p><h3 className="display mt-3 text-3xl">{product.title}</h3></div><p className="whitespace-nowrap font-mono text-sm font-bold text-[#89D7B7]">{money(product.price)}</p></div><p className="mt-3 text-sm leading-6 text-[#FFF4E1]/68">{product.summary}</p><Link href={`/shop/${product.slug}`} className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-[#89D7B7] hover:text-[#FFF4E1]">View product <ArrowRight className="size-3.5" /></Link></div></article>)}</div> : null}</section><section><div className="flex flex-col gap-4 border-y border-[#1A312C]/12 py-5 lg:flex-row lg:items-center lg:justify-between"><div><p className="eyebrow">Browse the catalogue</p><h2 className="display mt-2 text-3xl text-[#1A312C]">Find the right starting point.</h2></div><label className="relative block w-full max-w-md"><Search className="pointer-events-none absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-[#428475]" /><input value={search} onChange={event => setSearch(event.target.value)} className="form-field !pl-10 text-sm" placeholder="Search products" /></label></div><div className="mt-5 flex flex-wrap gap-2">{categories.map(item => <button key={item} type="button" onClick={() => setCategory(item)} className={`rounded-full px-4 py-2 text-xs font-semibold transition ${category === item ? "bg-[#1A312C] text-[#FFF4E1]" : "border border-[#1A312C]/12 bg-white/40 text-[#1A312C]/70 hover:bg-[#89D7B7]/25"}`}>{item}</button>)}</div>{filtered.length ? <div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(product => <article key={product.id} className="group overflow-hidden rounded-[1.15rem] border border-[#1A312C]/11 bg-white/60">{product.coverImageUrl ? <Link href={`/shop/${product.slug}`} className="block aspect-[1.24] overflow-hidden"><img src={product.coverImageUrl} alt={product.title} className="size-full object-cover transition duration-500 group-hover:scale-[1.035]" /></Link> : <Link href={`/shop/${product.slug}`} className="block aspect-[1.24] bg-[linear-gradient(135deg,#1A312C,#428475)]" />}<div className="p-5"><div className="flex items-start justify-between gap-3"><div><p className="font-mono text-[0.62rem] uppercase tracking-[0.1em] text-[#428475]">{product.category}</p><Link href={`/shop/${product.slug}`} className="mt-2 block text-base font-bold text-[#1A312C] hover:text-[#428475]">{product.title}</Link></div><p className="whitespace-nowrap font-mono text-sm font-semibold text-[#1A312C]">{money(product.price)}</p></div><p className="mt-3 line-clamp-2 text-sm leading-6 text-[#1A312C]/62">{product.summary}</p><Link href={`/shop/${product.slug}`} className="mt-5 inline-flex items-center gap-1.5 text-sm font-bold text-[#428475] hover:text-[#1A312C]">Request details <ArrowRight className="size-3.5" /></Link></div></article>)}</div> : <div className="py-16 text-center"><h3 className="display text-3xl">No matching products yet.</h3><button type="button" className="mt-3 text-sm font-bold text-[#428475]" onClick={() => { setSearch(""); setCategory("All products"); }}>Clear filters</button></div>}</section></> : <div className="rounded-[1.5rem] border border-dashed border-[#1A312C]/16 bg-white/45 px-8 py-16 text-center"><p className="eyebrow">Opening soon</p><h2 className="display mt-4 text-4xl text-[#1A312C]">The Digital Junction product catalogue is being prepared.</h2><p className="mx-auto mt-4 max-w-lg text-sm leading-6 text-[#1A312C]/62">This is a direct, owner-operated product library. New products will appear here as they are published by the studio.</p><Link href="/contact" className="button-primary buttonlike mt-7">Ask about a custom resource <ArrowRight className="size-4" /></Link></div>}</div></section></main></PublicLayout>;
}
