import { useAuth } from "@/_core/hooks/useAuth";
import DashboardLayout from "@/components/DashboardLayout";
import { ownerNavigation } from "@/data/ownerNavigation";
import { trpc } from "@/lib/trpc";
import { ArrowRight, Bell, Boxes, FileText, FolderKanban, KeyRound, Loader2, PackagePlus, Search, ShieldAlert, UsersRound } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";

export default function OwnerDashboard() {
  const { user } = useAuth({ scope: "owner" });
  const isOwner = user?.role === "admin";
  const [search, setSearch] = useState("");
  const products = trpc.portal.admin.products.list.useQuery(undefined, { enabled: isOwner });
  const projects = trpc.portal.admin.projects.useQuery(undefined, { enabled: isOwner });
  const clients = trpc.portal.admin.clients.useQuery(undefined, { enabled: isOwner });
  const inventory = useMemo(() => (products.data ?? []).filter(item => item.title.toLowerCase().includes(search.toLowerCase())), [products.data, search]);
  return <DashboardLayout navigation={ownerNavigation} title="DJDC Owner"><div className="mx-auto max-w-7xl space-y-6 py-2">{!isOwner ? <section className="grid min-h-[60vh] place-items-center rounded-[1.6rem] border border-[#1A312C]/12 bg-white"><div className="max-w-md p-8 text-center"><ShieldAlert className="mx-auto size-8 text-[#428475]" /><h1 className="display mt-5 text-3xl text-[#1A312C]">Owner access required.</h1><p className="mt-3 text-sm leading-6 text-[#1A312C]/65">Use the separate owner session to manage Digital Junction.</p></div></section> : <><header className="flex flex-col gap-4 rounded-[1.55rem] border border-[#1A312C]/10 bg-white p-4 sm:flex-row sm:items-center sm:justify-between"><label className="flex min-w-0 flex-1 items-center gap-3 rounded-xl border border-[#1A312C]/10 bg-[#FFF4E1]/60 px-4 py-3 sm:max-w-md"><Search className="size-4 text-[#428475]" /><input value={search} onChange={event => setSearch(event.target.value)} className="w-full bg-transparent text-sm outline-none placeholder:text-[#1A312C]/42" placeholder="Search inventory…" /></label><div className="flex items-center gap-3"><button type="button" className="grid size-10 place-items-center rounded-xl border border-[#1A312C]/10 text-[#1A312C]/66" aria-label="Notifications"><Bell className="size-4" /></button><Link href="/owner/inventory" className="button-primary buttonlike"><PackagePlus className="size-4" />Add new product</Link></div></header><section><p className="eyebrow">Admin dashboard overview</p><h1 className="display mt-2 text-4xl text-[#1A312C]">Run Digital Junction from one clear view.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[#1A312C]/65">Inventory, customer access, content, and product delivery are owner-controlled. Financial performance appears only when genuine sales records exist.</p></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={<Boxes className="size-5" />} label="Inventory listings" value={products.isLoading ? "…" : String(products.data?.length ?? 0)} detail="Live catalogue count" /><Metric icon={<FolderKanban className="size-5" />} label="Service projects" value={projects.isLoading ? "…" : String(projects.data?.length ?? 0)} detail="Owner-managed work" /><Metric icon={<UsersRound className="size-5" />} label="Customer accounts" value={clients.isLoading ? "…" : String(clients.data?.length ?? 0)} detail="Registered access" /><Metric icon={<FileText className="size-5" />} label="Sales records" value="—" detail="No real sales recorded" /></section><section className="grid gap-5 xl:grid-cols-[1.45fr_.85fr]"><article className="rounded-[1.55rem] border border-[#1A312C]/12 bg-white p-6"><div className="flex items-start justify-between gap-4"><div><p className="eyebrow">Inventory pulse</p><h2 className="display mt-1 text-3xl text-[#1A312C]">Listings at a glance</h2></div><Link href="/owner/inventory" className="button-quiet !min-h-9 !px-3 text-xs">Open inventory <ArrowRight className="size-3.5" /></Link></div>{products.isLoading ? <div className="grid min-h-48 place-items-center"><Loader2 className="size-6 animate-spin text-[#428475]" /></div> : inventory.length ? <div className="mt-6 grid gap-3">{inventory.slice(0, 5).map(product => <div key={product.id} className="flex items-center justify-between gap-4 rounded-xl border border-[#1A312C]/10 bg-[#FFF4E1]/55 p-4"><div><p className="font-bold text-[#1A312C]">{product.title}</p><p className="mt-1 text-xs text-[#1A312C]/58">{product.category} · {product.isPublished ? "Published" : "Draft"}</p></div><span className="rounded-full bg-[#89D7B7]/30 px-3 py-1 font-mono text-[0.58rem] uppercase tracking-[.1em] text-[#1A312C]/70">Inventory</span></div>)}</div> : <div className="mt-6 rounded-xl border border-dashed border-[#1A312C]/16 bg-[#FFF4E1]/60 p-6 text-sm leading-6 text-[#1A312C]/65">No real inventory listings yet. Create a Digital Junction product to populate this view.</div>}</article><aside className="rounded-[1.55rem] bg-[#1A312C] p-6 text-[#FFF4E1]"><p className="font-mono text-[0.62rem] uppercase tracking-[.14em] text-[#89D7B7]">Quick actions</p><h2 className="display mt-3 text-3xl">Keep operations moving.</h2><div className="mt-6 grid gap-3"><Quick href="/owner/inventory" icon={<Boxes className="size-4" />} label="Manage inventory" /><Quick href="/owner/customers" icon={<UsersRound className="size-4" />} label="Manage customers" /><Quick href="/owner/content" icon={<FileText className="size-4" />} label="Publish content" /><Quick href="/owner/product-access" icon={<KeyRound className="size-4" />} label="Grant product access" /></div></aside></section><section className="grid gap-5 lg:grid-cols-2"><article className="rounded-[1.55rem] border border-[#1A312C]/12 bg-white p-6"><p className="eyebrow">Sales area</p><h2 className="display mt-2 text-3xl text-[#1A312C]">Awaiting real records.</h2><p className="mt-4 text-sm leading-6 text-[#1A312C]/65">No revenue chart or transaction list is shown because Digital Junction has no real sales data in this workspace yet.</p><Link href="/owner/sales" className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-[#428475] hover:text-[#1A312C]">Open Sales workspace <ArrowRight className="size-4" /></Link></article><article className="rounded-[1.55rem] bg-[#89D7B7] p-6 text-[#1A312C]"><p className="eyebrow">Sample admin preview</p><h2 className="display mt-2 text-3xl">Design-ready, data-honest.</h2><p className="mt-4 text-sm leading-6 text-[#1A312C]/70">The operations shell is ready for your real inventory, customer access, content, vouchers, and sales. Placeholder views are clearly marked and never claim unrecorded business results.</p></article></section></>}</div></DashboardLayout>;
}
function Metric({ icon, label, value, detail }: { icon: React.ReactNode; label: string; value: string; detail: string }) { return <article className="rounded-[1.35rem] border border-[#1A312C]/12 bg-white p-5"><span className="grid size-10 place-items-center rounded-xl bg-[#89D7B7]/28 text-[#428475]">{icon}</span><p className="mt-5 font-mono text-[0.58rem] uppercase tracking-[.1em] text-[#1A312C]/52">{label}</p><p className="mt-1 text-2xl font-bold text-[#1A312C]">{value}</p><p className="mt-1 text-xs text-[#1A312C]/58">{detail}</p></article>; }
function Quick({ href, icon, label }: { href: string; icon: React.ReactNode; label: string }) { return <Link href={href} className="flex items-center justify-between rounded-xl border border-white/15 bg-white/[.06] px-4 py-3 text-sm font-bold text-[#FFF4E1] transition hover:bg-white/10"><span className="flex items-center gap-3">{icon}{label}</span><ArrowRight className="size-4 text-[#89D7B7]" /></Link>; }
